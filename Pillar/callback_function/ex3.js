// Evenet Listeners 

document.getElementById("clickme").addEventListener("click",function abc(){ // here name less function is also fine but inorder to find inside the container we  use named funtion
    console.log("I'm clicked");
})
