/*
    In the Previous ex1 you can see the setTimeout function that has callback funtion

    lets talk about,
    
    3. Blocking the main thread ;

    -> Here "Callstack" is called mainThread. blocking callstack is called Blocking the main thread ("callstack" that container) 

    -> You can see in google chrome we can see in the source -> callstack and other function's

    -> mentioned in Akshay saini video** must watch 

    -> while running the code each and every function stored and get deleted in the "Callstack"

    -> For setTime out firt That Parameter funtion will not available in call stack but after That Timer that 
        function appear as anonymous function Please Note in containor 


*/